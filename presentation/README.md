# MARKOUT UHI10 Presentation

`MARKOUT-UHI10.pptx` is the nine-slide judge deck for the token-independent Phase 9 checkpoint. Its narrative moves
from the fee-classification gap through mechanism, deterministic evidence, Reactive architecture, engineering proof,
and the exact remaining live gate.

## Evidence policy

- Every quantitative value comes from the committed Phase 6 trade tape and generated reports.
- The deck calls the loss metric a post-trade adverse-selection proxy, not exact LVR or individual LP PnL.
- The volatility-policy regression is visible rather than hidden.
- The final slide says that two public autonomous Lasna traces remain required.
- Speaker notes contain a `[Sources]` block on every slide.

## Visual verification

The final PowerPoint was rendered at `1600 × 900`, inspected slide-by-slide and as a montage, and passed the bundled
overflow checker with zero out-of-bounds elements. Connector direction, chart legibility, numeric wrapping, and footer
consistency were explicitly reviewed after a second render.

Do not replace the remaining-gate copy or create the final release tag until real explorer-backed traces exist.
